import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  fonts: {
    heading: 'Open Sans',
    body: `"Bebas NeueRaleway", sans-serif, Bitter, Roboto, Lato`
  }
})

export default theme
